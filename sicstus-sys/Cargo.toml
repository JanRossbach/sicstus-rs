[package]
name = "sicstus-sys"
version = "0.1.0"
edition = "2021"
description = " Low-level Rust bindings for SICStus Prolog."
categories = ["external-ffi-bindings"]


[build-dependencies]
bindgen = "0.65.1"
regex = "1.10.3"

[dependencies]
once_cell = "1.19.0"
